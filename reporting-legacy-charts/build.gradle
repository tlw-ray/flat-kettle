dependencies {
    compile (
            project(':reporting-classic-core'),
            project(':reporting-libraries-libswing'),
            libraries.'jfreechart',
            libraries.'jcommon',
    )
}
