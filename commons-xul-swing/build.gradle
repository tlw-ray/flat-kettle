dependencies {
    compile (
            project(':commons-xul-core'),
            libraries.'dom4j',
    )
    testCompile (
            libraries.'commons-collections',
            libraries.'jaxen',
    )
}
