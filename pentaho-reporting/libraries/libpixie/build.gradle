dependencies {
    compile (
            project(':pentaho-reporting:libraries:libbase'),
            libraries.'commons-logging',
    )
    testCompile (
            libraries.'hamcrest-core',
    )
}
