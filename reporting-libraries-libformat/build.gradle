dependencies {
    compile (
            project(':reporting-libraries-libbase'),
            project(':reporting-libraries-libloader'),
    )
    testCompile (
            libraries.'hamcrest-core',
            libraries.'junit',
    )
}
