dependencies {
    compile (
            libraries.'commons-lang3',
            libraries.'commons-dbcp',
    )
    testCompile (
            libraries.'junit',
            libraries.'mockito-all',
    )
}
