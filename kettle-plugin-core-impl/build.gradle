dependencies {
    compile (
            project(':kettle-core'),
            project(':kettle-engine'),
    )
//    provided (
//
//    )
    testCompile (
            project(path: ':kettle-core', configuration: 'tests'),
            project(path: ':kettle-engine', configuration: 'tests'),
            libraries.'h2',
    )
}
