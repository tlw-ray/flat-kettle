dependencies {
    compile (
            project(':reporting-libraries-libbase'),
    )
    testCompile (
            libraries.'hamcrest-core',
            libraries.'junit',
            libraries.'mockito-all',
    )
}
