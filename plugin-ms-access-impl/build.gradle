dependencies {
    compile (
            project(':kettle-core'),
            project(':kettle-engine'),
            libraries.'jackcess',
    )
//    provided (
//
//    )
    testCompile (
            project(path: ':kettle-engine', configuration: 'tests'),
            libraries.'mockito-all',
    )
}
